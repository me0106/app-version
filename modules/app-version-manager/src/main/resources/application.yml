spring:
  profiles:
    active: dev
  application:
    name: app-version-manager
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
mybatis:
  mapper-locations: classpath*:/mapper/*.xml